<?xml version="1.0" encoding="UTF-8"?>
<Theme version="1">
	<!-- 墙纸路径以及 背景颜色透明度 -->
	<!-- 文件夹缩略图 -->
	<!-- 文件夹缩略图底图 -->
	<!-- 文件夹缩略图顶罩(打开) -->
	<!-- 文件夹缩略图顶罩(关闭) -->
	<Foldericon bottom="appfunc_folderback_4_def3" top_open="appfunc_folder_open_4_def3" top_closed="appfunc_folder_close_up_4_def3">
	</Foldericon>
	<!-- 文件夹 -->
	<!-- 文件夹展开图 以及绘制方式 -->
	<!-- 文件夹展开时的编辑框以及绘制方式 -->
	<!-- 文件夹收起按钮 -->
	<!-- 整个Tab栏 -->
	<!-- 整个Tab栏背景图片(竖屏)以及绘制方式 -->
	<!-- 整个Tab栏背景图片(横屏)以及绘制方式 -->
	<!-- 单个Tab -->
	<!-- 单个Tab背景图片(竖屏)以及绘制方式 -->
	<!-- 单个Tab背景图片(横屏)以及绘制方式 -->
	<!-- 单个Tab选中时的图片(竖屏)以及绘制方式 -->
	<!-- 单个Tab选中时的图片(横屏)以及绘制方式 -->
	<!-- 单个Tab点击和聚焦时的图片(竖屏)以及绘制方式 -->
	<!-- 单个Tab点击和聚焦时的图片(横屏)以及绘制方式 -->
	<!-- Tab栏分割线开关数字 -->
	<!-- Tab屏幕自适应开关 -->
	<!-- 所有程序Tab图标 -->
	<!-- 未选中时图标 -->
	<!-- 选中时图标 -->
	<!-- 最近打开Tab图标 -->
	<!-- 未选中时图标 -->
	<!-- 选中时图标 -->
	<!-- 进程管理Tab图标 -->
	<!-- 未选中时图标 -->
	<!-- 选中时图标 -->
	<!-- Tab名称 -->
	<!-- 未选中时的字体颜色 -->
	<!-- 选中时的字体颜色 -->
	<!-- 字体与底部的间距(竖屏) -->
	<!-- 字体与底部的间距(横屏) -->
	<!-- Home图标 -->
	<!-- 未选中 -->
	<!-- 选中 -->
	<!-- 背景(竖屏)以及绘制方式 -->
	<!-- 背景(横屏)以及绘制方式 -->
	<!-- 背景填充颜色 -->
	<!-- 移动到桌面 -->
	<!-- 背景(竖屏)以及绘制方式 -->
	<!-- 背景(横屏)以及绘制方式 -->
	<!-- 文字颜色 -->
	<!-- 正在运行相关配置 -->
	<!-- 所有程序相关配置 -->
	<!-- 最近打开相关配置 -->
	<!-- 清空历史记录 -->
	<!-- 背景(竖屏)以及绘制方式 -->
	<!-- 背景(横屏)以及绘制方式 -->
	<!-- 背景填充颜色 -->
	<!-- 关闭所有程序 -->
	<!-- 背景(竖屏)以及绘制方式 -->
	<!-- 背景(横屏)以及绘制方式 -->
	<!-- 文本颜色 -->
	<!-- 指示器 -->
	<!--<Indicator indicator_h_current="screennow_4_def3" indicator_h="screenother_4_def3">-->
	<!--</Indicator>-->
	<!-- 应用程序文本颜色 -->
	<!-- 应用程序聚焦时的背景颜色 -->
	<!-- 跟主题相关的配置 -->
	<!-- 行列数格式数值 -->
</Theme>

